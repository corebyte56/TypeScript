// Declare a variable named 'isOpen' with boolean type
let isOpen: boolean = true;

// Example of type error (string cannot be assigned to boolean)
// let isClose: boolean = 'alex';

// Print the value of isOpen to the console
console.log(isOpen);