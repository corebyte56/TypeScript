    // TypeScript Example
    // In TypeScript we define the type of the array
    let numbers: number[] = [1, 2, 3, 4, 5];

    // In JavaScript we don't define the array type
    // let numbers = [1, 2, 3, 4, 5];

    // Loop through the array and print each number
    numbers.forEach((num) => {
    console.log(num);
    });